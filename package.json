{
  "name": "discoflare",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "dev:full": "nuxt build && wrangler dev .output/server/index.mjs --assets .output/public -c wrangler.dev.jsonc --port 3000",
    "dev:sandbox": "node scripts/dev-sandbox.mjs",
    "preview": "nuxt build && wrangler dev",
    "deploy": "nuxt build && pnpm db:migrate && wrangler deploy",
    "env:init": "node scripts/init-env.mjs",
    "postinstall": "nuxt prepare",
    "typecheck": "nuxt typecheck",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:migrate:local": "wrangler d1 migrations apply DB --local -c wrangler.dev.jsonc",
    "db:migrate": "wrangler d1 migrations apply DB --remote",
    "db:seed": "node scripts/seed.mjs",
    "cf:types": "wrangler types"
  },
  "cloudflare": {
    "bindings": {
      "AUTH_SECRET": { "description": "Generate a random 32+ character value. This signs login sessions." },
      "APP_NAME": { "description": "Name shown beside the Discoflare logo and in the page title." },
      "APP_TITLE": { "description": "Login headline. Use \\n to split it across two lines." },
      "APP_SUBTITLE": { "description": "Supporting copy shown below the login headline." },
      "TWITTER_CLIENT_ID": { "description": "Optional OAuth 2.0 client ID for Sign in with X. Configure `/api/auth/callback/twitter` as the callback path." },
      "TWITTER_CLIENT_SECRET": { "description": "Optional OAuth 2.0 client secret for Sign in with X." },
      "ADMIN_EMAIL": { "description": "Email for the first owner account." },
      "ADMIN_PASSWORD": { "description": "Strong password for the first owner account (8+ characters)." },
      "ADMIN_NAME": { "description": "Display name for the first owner account." }
    }
  },
  "dependencies": {
    "@better-auth/drizzle-adapter": "^1.7.2",
    "@cloudflare/realtimekit": "^2.0.2",
    "@iconify-json/ph": "^1.2.2",
    "@noble/hashes": "^2.4.0",
    "@nuxt/ui": "^4.11.0",
    "@pinia/nuxt": "^1.0.2",
    "@tanstack/vue-query": "^5.102.8",
    "@vueuse/core": "^14.4.0",
    "@vueuse/nuxt": "^14.4.0",
    "better-auth": "^1.7.2",
    "bignumber.js": "^11.1.5",
    "dayjs": "^1.11.23",
    "drizzle-orm": "^0.45.2",
    "nanoid": "^6.0.1",
    "nuxt": "^4.5.2",
    "pinia": "^4.0.3",
    "uuidv7": "^1.2.1",
    "vue": "^3.5.42",
    "vue-router": "^5.3.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260902.1",
    "@nuxt/eslint": "^1.17.0",
    "@vue/test-utils": "^2.5.0",
    "drizzle-kit": "^0.31.10",
    "eslint": "^10.9.1",
    "happy-dom": "^20.13.1",
    "miniflare": "5.20260831.0-alpha",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.9.2",
    "vitest": "^4.1.11",
    "vue-tsc": "^3.3.11",
    "wrangler": "^4.128.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "unrs-resolver",
      "vue-demi",
      "workerd"
    ]
  }
}
